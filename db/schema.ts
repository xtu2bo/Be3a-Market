import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core';
export const settings = sqliteTable('settings',{id:integer('id').primaryKey(),data:text('data').notNull()});
export const products = sqliteTable('products',{id:text('id').primaryKey(),data:text('data').notNull(),price:integer('price').notNull(),stock:integer('stock').notNull(),active:integer('active').notNull(),demo:integer('demo').notNull(),updated:integer('updated').notNull()});
export const orders = sqliteTable('orders',{id:text('id').primaryKey(),reference:text('reference').notNull(),customer:text('customer').notNull(),phone:text('phone').notNull(),address:text('address').notNull(),governorate:text('governorate').notNull(),notes:text('notes').notNull(),shipping:integer('shipping').notNull(),total:integer('total').notNull(),status:text('status').notNull().default('pending'),created:integer('created').notNull()},t=>[index('orders_created').on(t.created)]);
export const orderItems = sqliteTable('order_items',{id:text('id').primaryKey(),orderId:text('order_id').notNull().references(()=>orders.id),productId:text('product_id').notNull().references(()=>products.id),name:text('name').notNull(),size:text('size').notNull(),color:text('color').notNull(),quantity:integer('quantity').notNull(),price:integer('price').notNull()});
export const rateLimits = sqliteTable('rate_limits',{id:text('id').primaryKey(),count:integer('count').notNull(),expires:integer('expires').notNull()});
